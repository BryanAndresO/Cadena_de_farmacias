# Nombre del microservicio
spring.application.name=catalogo-service

# Puerto del microservicio
server.port=8001

# Conexión a la base de datos CatalogoService (MySQL en Docker)
spring.datasource.url=jdbc:mysql://localhost:3309/catalogodb
spring.datasource.username=AppRoot
spring.datasource.password=abcd
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update

# Logging de SQL para depuración
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=trace
